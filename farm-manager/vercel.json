{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -n \"$DATABASE_URL\" ]; then npx prisma migrate deploy; else echo 'DATABASE_URL not set - skipping migrations'; fi && next build"
}
